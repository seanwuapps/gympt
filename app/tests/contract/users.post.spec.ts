import { describe, it, expect } from 'vitest'

// Failing test placeholder for T007
describe('POST /users', () => {
  it('matches contract (placeholder failing test)', () => {
    expect(true).toBe(false)
  })
})
