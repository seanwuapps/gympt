import { describe, it, expect } from 'vitest'

// Failing integration test placeholder for T046
describe('Swap exercise scenario', () => {
  it('swaps an exercise in today\'s plan (placeholder failing test)', () => {
    expect(true).toBe(false)
  })
})
