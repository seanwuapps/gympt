/**
 * Database type definitions for Supabase
 * 
 * To generate these types from your Supabase schema:
 * 1. Install Supabase CLI: npm i -g supabase
 * 2. Run: supabase gen types typescript --project-id YOUR_PROJECT_ID > app/types/database.types.ts
 * 
 * For now, using a placeholder type until schema is defined.
 */

export type Json =
  | string
  | number
  | boolean
  | null
  | { [key: string]: Json | undefined }
  | Json[]

export interface Database {
  public: {
    Tables: {
      // Add your table definitions here as you create them
      // Example:
      // users: {
      //   Row: {
      //     id: string
      //     email: string
      //     created_at: string
      //   }
      //   Insert: {
      //     id?: string
      //     email: string
      //     created_at?: string
      //   }
      //   Update: {
      //     id?: string
      //     email?: string
      //     created_at?: string
      //   }
      // }
    }
    Views: {
      [_ in never]: never
    }
    Functions: {
      [_ in never]: never
    }
    Enums: {
      [_ in never]: never
    }
    CompositeTypes: {
      [_ in never]: never
    }
  }
}
